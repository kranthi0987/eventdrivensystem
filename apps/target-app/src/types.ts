export interface Event {
    id: string;
    name: string;
    body: string;
    timestamp: string;
    brand: string;
}

export interface EventInput {
    id: string;
    name: string;
    body: string;
    timestamp: string;
    brand: string;
}